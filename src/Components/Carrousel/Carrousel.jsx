import Experience from "../Experience-Card/Experience.jsx"
import experiences from "../../Data/experiences.json"

export default function Carrousel() {
  //   return <>{experiences && experiences.map((experience) => <Experience />)}</>
  return <Experience />
}
